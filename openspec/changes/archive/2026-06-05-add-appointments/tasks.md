## 1. Домен

- [x] 1.1 Добавить в `src/domain/specialist.py` поля настроек расписания (`timezone`, `day_start`, `day_end`, `slot_minutes`) в сущность `Specialist` и в протокол репозитория метод обновления настроек
- [x] 1.2 Создать `src/domain/appointment.py`: сущность `Appointment` (`id`, `specialist_id`, `client_id`, `starts_at`, `comment`, `created_at`, `updated_at`) и протокол `AppointmentsRepo` (add, get_for_specialist, list_future/ list_past постранично — на уровне специалиста и клиента, update_starts_at, delete)
- [x] 1.3 Добавить чистые хелперы времени: курированный список российских таймзон, генерация слотов из (`day_start`, `day_end`, `slot_minutes`), парсинг/валидация произвольного `ЧЧ:ММ`, маппинг русских месяцев (родительный) и дней недели

## 2. Инфраструктура и миграция

- [x] 2.1 Создать `src/infrastructure/appointments_repo.py`: `AppointmentORM` (таблица `appointments`, FK на `specialists` и `clients`, индексы `(specialist_id, starts_at)` и `(client_id, starts_at)`), `to_domain`, реализация `AppointmentsRepo` (фильтрация по `specialist_id`, пагинация «limit+1»)
- [x] 2.2 Расширить `SpecialistORM` колонками `timezone`, `day_start`, `day_end`, `slot_minutes` и реализацию репозитория методом сохранения настроек
- [x] 2.3 Alembic-ревизия: `CREATE TABLE appointments` + индексы; `ALTER TABLE specialists` добавляет 4 колонки с server-default (`Asia/Yekaterinburg`, `09:00`, `20:00`, `60`); проверить up/down

## 3. Сервисы

- [x] 3.1 `src/services/appointments.py`: создание записи (настенное время в TZ специалиста → UTC, запрет прошедшей даты), перенос, удаление, выборки будущих/истории на уровне специалиста и клиента; логирование событий (`appointment.created`, `.rescheduled`, `.deleted`)
- [x] 3.2 Расширить `src/services/specialists.py` (или создать use-case) получением и обновлением настроек расписания с валидацией значений
- [x] 3.3 Хелпер расчёта «сегодня» в TZ специалиста и группировки будущих записей по дням для ленты расписания

## 4. Бот: тексты и настройки

- [x] 4.1 Добавить тексты в `src/bot/messages.toml` и дата-классы в `src/bot/messages.py` для расписания, настроек, карточки записи, подтверждения удаления, ошибок ввода времени, пустых состояний
- [x] 4.2 Добавить кнопки `📅 Расписание` и `⚙️ Настройки` в `build_main_keyboard`
- [x] 4.3 Роутер настроек: меню настроек, выбор таймзоны из списка рос. зон, изменение начала/конца дня и длины слота (FSM-ввод с валидацией)

## 5. Бот: расписание

- [x] 5.1 Инлайн-календарь: рендер месяца, навигация по месяцам через `callback_data`, неактивные прошедшие дни, выбор дня
- [x] 5.2 Сетка слотов из настроек специалиста + кнопка «Другое время» (FSM-шаг `ЧЧ:ММ` с валидацией), затем опциональный комментарий (паттерн «Пропустить/Отмена») → создание записи и показ карточки
- [x] 5.3 Карточка записи с кнопками `Перенести` / `Удалить`; перенос переиспользует календарь+время и обновляет `starts_at`
- [x] 5.4 Подтверждение удаления `[Да, удалить] [Отмена]` → жёсткое удаление
- [x] 5.5 Раздел `📅 Расписание` из главного меню: будущие записи, сгруппированные по дням, и история с пагинацией
- [x] 5.6 В карточке клиента — кнопки «Будущие записи» и «История записей» этого клиента (списки + пагинация истории)
- [x] 5.7 Зарегистрировать роутеры расписания и настроек в `src/bot/dispatcher.py`

## 6. Тесты

- [x] 6.1 Доменные хелперы: генерация слотов (включая пример 14:00–19:40/50), парсинг `ЧЧ:ММ`, конверсия настенного времени ↔ UTC, «сегодня» в TZ, форматирование русских дат
- [x] 6.2 Сервисы: создание (UTC из настенного времени), запрет прошедшей даты, перенос, удаление, выборки будущих/истории и группировка по дням, изоляция по `specialist_id`
- [x] 6.3 Хендлеры (фейковые `Message`/`CallbackQuery`): создание через пикер, произвольное время и его ошибки, перенос, подтверждение/отмена удаления, настройки (смена TZ и параметров дня)
- [x] 6.4 Репозитории: CRUD `appointments`, пагинация, сохранение настроек специалиста

## 7. Документация

- [x] 7.1 `docs/decisions/2026-06-04_appointment_time_in_utc_per_specialist_tz.md` — что/зачем/цена (UTC + TZ специалиста, «сегодня» в TZ, не доверяем времени сервера)
- [x] 7.2 Обновить `docs/database.md` (таблица `appointments` + новые колонки `specialists` + миграция), `docs/bot.md` (роутеры расписания и настроек), `docs/features.md` (записи и настройки расписания)
- [x] 7.3 Обновить `README.md` (новые возможности и, при необходимости, разделы меню)

## 8. Финал

- [x] 8.1 `make check` зелёный (формат, линт, типы, 100% покрытие)
- [x] 8.2 `openspec validate add-appointments` проходит
