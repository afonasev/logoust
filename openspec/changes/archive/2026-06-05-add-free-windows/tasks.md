## 1. Домен: рабочие дни и ближайшие рабочие дни

- [x] 1.1 В `src/domain/specialist.py` добавить поле `working_days: str` и `DEFAULT_WORKING_DAYS = "0,1,2,3,4"`
- [x] 1.2 В `src/domain/schedule.py` добавить `parse_working_days(raw) -> list[int]` (отсортированный, без дублей, 0=Пн…6=Вс) и `format_working_days(days) -> str` (канонизация)
- [x] 1.3 В `src/domain/schedule.py` добавить `next_working_days(today, working_days, count) -> list[date]` с guard на пустой набор (возврат `[]`)
- [x] 1.4 Добавить короткие подписи дней недели для тоггла (если ещё нет — `RU_WEEKDAYS_SHORT` уже есть, переиспользовать)
- [x] 1.5 Тесты на `parse/format` round-trip и `next_working_days` (включая пропуск выходных, пустой набор, today как рабочий день)

## 2. Миграция

- [x] 2.1 Новая Alembic-миграция: колонка `working_days` в `specialists`, `String`, `nullable=False`, `server_default="0,1,2,3,4"`; downgrade — `drop_column`
- [x] 2.2 Обновить тест миграций (`tests/test_migration.py`): новая колонка и дефолт у существующей записи

## 3. Инфраструктура

- [x] 3.1 В ORM-модели специалиста (`src/infrastructure/specialists_repo.py`) добавить колонку `working_days` и проброс в доменный `Specialist`
- [x] 3.2 Тест репозитория: round-trip `working_days`, дефолт

## 4. Сервисы

- [x] 4.1 В `src/services/specialists.py` добавить операцию тоггла рабочего дня: прочитать набор → инвертировать день → сохранить канонизированную строку через `update_settings`; залогировать `specialist.setting_updated`
- [x] 4.2 В `src/services/appointments.py` добавить функцию расчёта свободных окон на ближайшие 5 рабочих дней: для каждого дня `generate_slots − taken_slot_times`, для сегодня отбросить слоты `<= now` в таймзоне; вернуть структуру «день → список свободных HH:MM»
- [x] 4.3 Тесты сервисов: тоггл дня; свободные окна (вычитание занятых, пропуск выходных, скрытие прошедших окон сегодня, день без окон, пустой `working_days`)

## 5. Бот: настройки (рабочие дни)

- [x] 5.1 В `src/bot/messages.toml` добавить тексты: подпись настройки рабочих дней, кнопку/заголовок тоггла
- [x] 5.2 В `src/bot/handlers/settings.py` добавить мульти-тоггл рабочих дней (7 кнопок ✅/⬜ + Назад), хендлер переключения с немедленным сохранением и перерисовкой; вывести строку рабочих дней в `render_settings`
- [x] 5.3 Тесты хендлера настроек: переключение дня сохраняет набор и перерисовывает меню

## 6. Бот: раздел «Окна»

- [x] 6.1 В `src/bot/messages.toml` добавить тексты раздела «Окна»: кнопку меню, заголовок, строку «свободных окон нет», сообщение «рабочие дни не заданы»
- [x] 6.2 В `src/bot/handlers/clients.py` добавить кнопку «Окна» в `build_main_keyboard` (верхний ряд)
- [x] 6.3 Новый хендлер раздела «Окна» (`src/bot/handlers/`): по нажатию кнопки рендерит свободные окна на 5 рабочих дней; ветка «рабочие дни не заданы»
- [x] 6.4 Зарегистрировать router нового хендлера в `src/bot/dispatcher.py`
- [x] 6.5 Тесты хендлера «Окна»: успешный рендер, день без окон, пустой `working_days`, скрытие прошедших окон сегодня (на уровне сервиса)

## 7. Документация

- [x] 7.1 `docs/database.md` — колонка `working_days`, раздел миграций
- [x] 7.2 `docs/bot.md` — кнопка «Окна», новый хендлер, тоггл рабочих дней в настройках
- [x] 7.3 `docs/features.md` — раздел «Окна» и рабочие дни
- [x] 7.4 `README.md` — раздел «Ключевые возможности» (новая кнопка/возможность)
- [x] 7.5 `openspec/specs/` — синхронизировать `availability` и `specialists` при архивации change

## 8. Финал

- [x] 8.1 `make check` — формат, линт, типы, 100% покрытие
