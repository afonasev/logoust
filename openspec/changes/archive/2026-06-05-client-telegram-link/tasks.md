## 1. Данные и миграция

- [x] 1.1 Добавить в ORM-модель `clients` (`src/infrastructure/clients_repo.py` или модуль моделей) колонки `telegram_chat_id` (BIGINT, nullable), `invite_token` (Text, nullable, UNIQUE), `linked_at` (timestamp, nullable)
- [x] 1.2 Сгенерировать Alembic-миграцию: добавить три колонки + уникальный индекс на `invite_token`; существующие строки → NULL
- [x] 1.3 Прогнать миграцию локально (`make run`/alembic upgrade) и убедиться, что схема применяется и откатывается

## 2. Domain

- [x] 2.1 Расширить `Client` (`src/domain/client.py`) полями `telegram_chat_id: int | None`, `invite_token: str | None`, `linked_at: datetime | None`
- [x] 2.2 Добавить в протокол `ClientsRepo` методы `set_invite_token`, `find_by_invite_token`, `link_telegram` (idempotent rebind: перезапись `telegram_chat_id` и `linked_at`)

## 3. Infrastructure

- [x] 3.1 Реализовать новые методы репозитория в `SqlAlchemyClientsRepo`, сохранив изоляцию по `specialist_id` для `set_invite_token`
- [x] 3.2 Маппинг новых колонок в обе стороны (ORM ↔ доменный `Client`)

## 4. Services

- [x] 4.1 Use-case генерации приглашения: лениво создаёт `cli_`-токен (переиспользует существующий), возвращает токен; лог-событие `client.invite_created`
- [x] 4.2 Use-case привязки по токену: ищет карточку по `invite_token`, делает idempotent rebind, лог-событие `client.linked`; на неизвестный токен — отдельный результат/ветка

## 5. Bot — онбординг клиента (start.py)

- [x] 5.1 Роутинг по префиксу `cli_`: в `/start`-хендлере и fallback-обработчике вставленного текста/ссылки отделить клиентский токен от specialist-токена
- [x] 5.2 Сборка deep-link клиента (`build_client_start_link` в `src/bot/deeplink.py`). Отклонение от плана: ссылка строится из `settings.TELEGRAM_BOT_USERNAME` (как в существующем `cli/create_invite.py`), а не через `create_start_link(bot, …)` — без лишнего API-вызова `bot.get_me()` и тестируемо без живого `Bot`.
- [x] 5.3 Ответ клиенту на успешную привязку и на неизвестный `cli_`-токен; специалист-онбординг не затронут (regression-проверка)

## 6. Bot — карточка клиента

- [x] 6.1 Кнопка приглашения в карточке: «Пригласить в бота» при `telegram_chat_id IS NULL`, «✅ Привязан (переотправить приглашение)» при привязке
- [x] 6.2 По нажатию — вызвать use-case приглашения и прислать специалисту пересылаемое сообщение со ссылкой и подсказкой клиенту нажать «Запустить»
- [x] 6.3 Предложить приглашение сразу после успешного создания клиента

## 7. Тексты

- [x] 7.1 Добавить в `src/bot/messages.toml` ключи: пересылаемое приглашение (с подсказкой про «Запустить»), подтверждение привязки клиенту, недействительный клиентский токен, подписи кнопок карточки

## 8. Тесты

- [x] 8.1 Сервис: генерация токена (создание/переиспользование), привязка (успех, rebind, неизвестный токен), изоляция по владельцу
- [x] 8.2 Хендлер `start`: `cli_`-токен → привязка; specialist-токен по-прежнему работает; обычный текст игнорируется; один аккаунт привязан к двум карточкам
- [x] 8.3 Хендлер карточки: состояние кнопки (привязан/нет), отправка приглашения; покрытие 100%

## 9. Документация

- [x] 9.1 Обновить `openspec/specs/clients/spec.md` (синхронизация delta) и прогнать `openspec validate clients`
- [x] 9.2 Обновить `docs/database.md` (новые колонки + индекс + раздел миграций)
- [x] 9.3 Обновить `docs/bot.md` (роутинг `cli_`, кнопка карточки) и `docs/features.md` (сценарий приглашения клиента)
- [x] 9.4 Обновить `README.md`, если затронуты возможности/структура верхнего уровня

## 10. Финальная проверка

- [x] 10.1 `make check` — формат, линт, типы, тесты с 100% покрытием
- [x] 10.2 Ручной прогон сценария: пригласить клиента → перейти по ссылке тем же аккаунтом → увидеть привязку в карточке
