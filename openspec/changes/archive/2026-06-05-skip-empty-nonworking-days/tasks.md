## 1. Сервис: ближайший показываемый день и день старта

- [x] 1.1 В `src/services/appointments.py` добавить `adjacent_shown_day(repo, *, specialist_id, working_days: set[int], tz, day, forward: bool) -> date | None`: ближайший в направлении день, который рабочий **или** содержит запись (рабочий-кандидат — чистый расчёт от `day±1`, кандидат-с-записью — один запрос `list_future_/list_past_for_specialist`), вернуть более раннюю (вперёд) / более позднюю (назад) дату или `None`
- [x] 1.2 В `src/services/appointments.py` добавить `schedule_landing_day(repo, *, specialist_id, working_days, tz, today) -> date`: `today`, если рабочий или есть записи на сегодня; иначе ближайший вперёд показываемый день; иначе `today`
- [x] 1.3 Тесты сервиса: пропуск пустых выходных вперёд/назад; нерабочий день с записью не пропускается; пустой `working_days` (навигация только по записям, иначе `None`); landing на рабочий сегодня, landing с пустого нерабочего сегодня на ближайший рабочий, landing без рабочих дней и записей → сегодня

## 2. Бот: навигация дневного вида

- [x] 2.1 В `src/bot/handlers/schedule.py::_day_view` вычислять цели `prev`/`next` через `adjacent_shown_day` и день старта (`day=None`) через `schedule_landing_day`
- [x] 2.2 В `_day_keyboard` принимать готовые `prev: date | None` / `next: date | None` и рисовать стрелку только при не-`None`
- [x] 2.3 Тесты хендлера: ▶ ведёт на ближайший рабочий день (пропуск выходных); ◀ симметрично; нерабочий день с записью доступен; старт раздела с пустого нерабочего сегодня открывает рабочий день; скрытие стрелки при отсутствии цели

## 3. Документация

- [x] 3.1 `docs/bot.md` — поведение ◀/▶ и старта раздела с учётом рабочих дней
- [x] 3.2 `docs/features.md` — раздел «Записи клиентов на приём»: навигация пропускает пустые нерабочие дни
- [x] 3.3 `openspec/specs/appointments/spec.md` — синхронизировать при архивации change

## 4. Финал

- [x] 4.1 `make check` — формат, линт, типы, 100% покрытие
