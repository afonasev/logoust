## 1. Предусловие и каркас навигации

- [x] 1.1 Убедиться, что change `appointment-notify` слит в его переписанном виде:
      «Уведомить клиента» — отдельный шаг Да/Нет, а не кнопка рядом с «Назад» (иначе хелпер
      придётся переделывать под уходящую кнопку-сиблинг)
- [x] 1.2 Создать нейтральный модуль `src/bot/navigation.py` (без импортов из конкретных
      хендлеров крест-накрест) с реестром: `back`-префикс → корутина, возвращающая
      `(text, InlineKeyboardMarkup)`
- [x] 1.3 Добавить fallback: неизвестный/пустой `back` → сегодняшний день расписания
      (`sched:feed`)

## 2. Чистые билдеры целей возврата

- [x] 2.1 Переиспользовать существующие билдеры: `_day_view` (`schedule.py`),
      `_active_view` (`clients.py`), вью карточки клиента (`clients.py`)
- [x] 2.2 Извлечь чистый `(text, keyboard)`-билдер для `recur:card` (поддержать составной
      ключ `series_id` + `origin_date` при парсинге префикса)
- [x] 2.3 Извлечь чистый `(text, keyboard)`-билдер для `sched:chist` (история записей
      клиента с пагинацией)
- [x] 2.4 Извлечь чистый `(text, keyboard)`-билдер для `clients:arch` (архивный список)
- [x] 2.5 Подключить все билдеры в реестр `navigation.py` по префиксам:
      `sched:day_view`, `sched:feed`, `sched:chist`, `clients:card`, `clients:active`,
      `clients:arch`, `recur:card`

## 3. Единый хелпер пост-действия

- [x] 3.1 Реализовать хелпер: ① отправить отдельное сообщение-результат (остаётся в
      истории) → ② отрендерить цель `back` через реестр и отправить меню новым сообщением
      внизу (самый свежий экран)
- [x] 3.2 Для колбэк-флоу погасить исходную карточку с неактуальными кнопками (edit в
      текст результата без клавиатуры либо снять клавиатуру) — без «мёртвых» кнопок

## 4. Перевод действий на хелпер

- [x] 4.1 `do_delete` (удаление разовой записи) → хелпер; убрать `_deleted_keyboard`
- [x] 4.2 `_do_move` (перенос регулярной) → хелпер
- [x] 4.3 `do_stop` (остановка серии) → хелпер
- [x] 4.4 `do_skip` (пропуск даты) → хелпер
- [x] 4.5 `cancel` (отмена флоу) → хелпер
- [x] 4.6 Привести к хелперу уже-навигирующие: `_do_reschedule` (перенос разовой),
      `archive`/`restore` клиента — без изменения видимого поведения
- [x] 4.7 Удалить `_deleted_keyboard`, если после миграции он больше не используется

## 5. Тесты

- [x] 5.1 Тест авто-возврата для каждого действия (удаление/перенос/стоп/пропуск/отмена):
      приходит отдельное сообщение-результат + открывается верная цель
- [x] 5.2 Тест кросс-роутерной цели: удаление записи, открытой из карточки клиента, ведёт
      к карточке клиента (раздел картотеки из раздела расписания)
- [x] 5.3 Тест fallback: неопределённая `back` → сегодняшний день расписания
- [x] 5.4 Тест парсера составного префикса `recur:card:<series>:<date>`
- [x] 5.5 Регресс: подтверждения (Да/Отмена) не затронуты — тупиковый экран не появляется
      только после необратимого действия, а не на шаге подтверждения

## 6. Документация

- [x] 6.1 Обновить `docs/bot.md` (контракт пост-действенной навигации, модуль
      `navigation.py`)
- [ ] 6.2 Синхронизировать `openspec/specs/menu-navigation/spec.md` (через `/opsx:sync`
      или вручную) — выполняется на шаге архивации change
- [x] 6.3 `make check` зелёный (формат, линт, типы, 100% покрытие)
