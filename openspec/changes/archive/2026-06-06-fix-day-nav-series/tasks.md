## 1. Сервис: series-aware навигация

- [x] 1.1 Добавить в `src/services/recurring.py` хелпер поиска ближайшего дня приземления occurrence активной серии относительно `day` в заданном направлении (вперёд — минимальная дата `> day` в пределах `_NEXT_OCCURRENCE_HORIZON`; назад — пусто, т.к. прошлое материализовано), переиспользуя `occurrences_landing_in`. Учитывает пропуски/переносы и только активные серии.
- [x] 1.2 Расширить `adjacent_shown_day` в `src/services/appointments.py` необязательным параметром `series: SeriesContext | None = None`; включить день приземления ближайшей occurrence серии в набор кандидатов наравне с рабочим днём и реальной записью; итог по-прежнему `min` (вперёд) / `max` (назад) среди непустых кандидатов.
- [x] 1.3 Не менять `_nearest_appt_day` (остаётся источником только реальных записей).

## 2. Бот: прокидывание контекста

- [x] 2.1 В `_day_view` (`src/bot/handlers/schedule.py`) передать уже загруженный `SeriesContext` в оба вызова `adjacent_shown_day` (`prev_day` и `next_day`) без дополнительных запросов к БД.

## 3. Тесты

- [x] 3.1 Навигация «следующий день» доходит до нерабочего дня с перенесённой на него occurrence серии (нет реальных записей) — воспроизводит исходный баг (серия по вт, перенос ближайшей на вс; из субботы next ведёт на вс, а не перешагивает на понедельник).
- [x] 3.2 Навигация доходит до нерабочего дня с обычным (непереносным) повтором серии, чей weekday вне `working_days`.
- [x] 3.3 Регрессия: пустые нерабочие дни без записей и без серий по-прежнему пропускаются; направление без показываемого дня даёт `None` (кнопка скрыта).
- [x] 3.4 «Предыдущий день» не предлагает виртуальную occurrence (прошлое — только реальные строки).

## 4. Спека и документация

- [x] 4.1 Применить delta к `openspec/specs/appointments/spec.md` (определение «показываемого» дня + сценарий навигации до дня с повтором серии); `openspec validate appointments`.
- [x] 4.2 Обновить `docs/features.md` / `docs/bot.md`, если описано поведение навигации по дням.

## 5. Проверка

- [x] 5.1 `make check` зелёный (формат, линт, типы, 100% покрытие).
