## 1. Тексты (messages.toml)

- [x] 1.1 Добавить в секцию `settings` строку `btn_cancel = "Отмена"` (если ещё нет)
- [x] 1.2 Добавить в секцию `settings` формат-строку `value_now = "\n\nСейчас: {current}"`

## 2. Настройки: отмена + текущее значение (settings.py)

- [x] 2.1 Добавить маппинг `SettingField → getter(Specialist) -> str` для текущего значения каждого из 7 полей
- [x] 2.2 Добавить клавиатуру шага ввода с одной кнопкой «Отмена» (`callback_data=_CB_MENU`)
- [x] 2.3 В `ask_value` загрузить специалиста, дописать к промпту суффикс `value_now` с текущим значением и прикрепить клавиатуру «Отмена»
- [x] 2.4 Убедиться, что «Отмена» уходит в существующий `open_menu` (сброс state + рендер меню), новый обработчик не нужен

## 3. Отложенное уведомление: отмена (schedule.py)

- [x] 3.1 Добавить колбэк «Отмена» для шага `notify_custom_time` и его клавиатуру; прикрепить её в `notify_custom`
- [x] 3.2 Обработчик отмены: `state.set_state(None)` (сохранить `_NOTIFY_DATA_KEY`), переиспользовать рендер `ask_when` (экран выбора момента); ничего не ставить в очередь
- [x] 3.3 Зарегистрировать колбэк в `build_router`

## 4. Тесты

- [x] 4.1 settings: вход в каждый/репрезентативный шаг ввода показывает текущее значение и кнопку «Отмена»
- [x] 4.2 settings: нажатие «Отмена» сбрасывает state и показывает меню, значение не изменилось
- [x] 4.3 schedule: шаг `notify_custom_time` показывает кнопку «Отмена»
- [x] 4.4 schedule: «Отмена» возвращает к выбору момента, очередь отложенных уведомлений пуста

## 5. Документация

- [x] 5.1 `docs/bot.md` — отметить кнопку «Отмена» и показ текущего значения на шагах ввода настроек + отмену на шаге своего времени
- [x] 5.2 Синхронизировать `openspec/specs/specialists` и `openspec/specs/deferred-client-notify` с delta-спеками этого change (через `/opsx:archive` при завершении)

## 6. Проверка

- [x] 6.1 `make check` зелёный (формат, линт, типы, 100% покрытие)
