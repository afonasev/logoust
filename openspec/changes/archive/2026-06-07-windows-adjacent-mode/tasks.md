## 1. Сервис: режим расчёта окон

- [x] 1.1 Добавить параметр режима в `list_free_windows` (`src/services/appointments.py`): флаг `adjacent: bool = False` (keyword-only)
- [x] 1.2 В режиме `adjacent` фильтровать свободные слоты по соседству: оставлять слот, если предыдущий или следующий слот сетки входит в `taken`; границы дня не считать соседом
- [x] 1.3 Сохранить существующий порядок: фильтр соседства по полной сетке выполняется до фильтра прошедших слотов сегодняшнего дня
- [x] 1.4 Тесты сервиса: смежные окна вокруг занятых слотов, пустой день → пусто в `adjacent`, занятый якорь в прошлом (сегодня) даёт будущего соседа, режим `all` без регрессий

## 2. Тексты

- [x] 2.1 Добавить в `[windows]` в `src/bot/messages.toml` подписи кнопок режимов (`button_all`, `button_adjacent`)
- [x] 2.2 Отразить новые поля в `WindowsMessages` (`src/bot/messages.py`)

## 3. Хендлер и переключение режима

- [x] 3.1 Вынести сборку окон по режиму в общий приватный метод (загрузка specialist + series + `list_free_windows`)
- [x] 3.2 `WindowsHandlers.show` рендерит дефолтный режим «Все окна» с inline-клавиатурой переключения
- [x] 3.3 Построить клавиатуру двух кнопок с пометкой активного режима; `callback_data` `windows:all` / `windows:adjacent`
- [x] 3.4 Новый callback-хендлер: читает режим из `callback_data`, пересобирает окна, `edit_text` того же сообщения
- [x] 3.5 Зарегистрировать `router.callback_query.middleware(SpecialistMiddleware(...))` и оба callback-хендлера в `build_router`
- [x] 3.6 `render_windows` принимает активный режим (для пометки кнопки/клавиатуры)
- [x] 3.7 Тесты хендлера: дефолт `show` отдаёт «Все» + клавиатуру; переключение редактирует сообщение и меняет активную пометку; пустой набор рабочих дней по-прежнему даёт подсказку

## 4. Документация

- [x] 4.1 `openspec/specs/availability/spec.md` — синхронизировать с дельтой (через `/opsx:archive` при завершении)
- [x] 4.2 `docs/features.md` — описать два режима окон
- [x] 4.3 `docs/bot.md` — новый callback и тексты кнопок раздела «Окна»
- [x] 4.4 `README.md` — упомянуть режим «смежные окна» в «Ключевых возможностях»

## 5. Проверка

- [x] 5.1 `make check` — формат, линт, типы, 100% покрытие
- [x] 5.2 `openspec validate windows-adjacent-mode`
