## 1. Доменное правило занятости

- [x] 1.1 Добавить в `src/domain/schedule.py` чистую функцию `occupied_grid_slots(grid, starts, slot_minutes) -> set[str]`: ячейка занята, если `|начало − слот| < slot_minutes` (минуты)
- [x] 1.2 Юнит-тесты на правило: 14:10/30мин → {14:00, 14:30}; 14:00/30мин → {14:00}; границы 13:30 и 15:00 свободны; пустой `starts`

## 2. Исключение правимого слота из занятости

- [x] 2.1 Добавить параметр `exclude_slot_id: int | None` в `slot_taken_times` (`src/services/recurring.py`) — пропускать вклад этого слота
- [x] 2.2 Пробросить `exclude_slot_id` через `taken_slot_times` (`src/services/appointments.py`) в цикл по `series.slots`
- [x] 2.3 Тесты: правимый слот не попадает в набор занятых, чужие повторы и разовые записи попадают

## 3. Клавиатуры слотов и текстовый список дня

- [x] 3.1 Перевести `build_slots_keyboard` и `build_recur_slots_keyboard` на приём набора занятых ячеек (результат `occupied_grid_slots`), а не набора начал
- [x] 3.2 Добавить третий маркер в `build_recur_slots_keyboard` через опциональный `current: str | None` (приоритет над занято/свободно)
- [x] 3.3 Добавить маркер «текущее время» и заголовок/строки списка записей дня в `src/bot/messages.toml`
- [x] 3.4 Реализовать чистый рендер `render_day_appointments_list(day, appts, names) -> str`: заголовок «дата, день недели» + строки «время — имя (комментарий)», сортировка по времени
- [x] 3.5 Тесты рендера: формат строк, off-grid запись присутствует, пустой день

## 4. Пикеры слотов в хендлере

- [x] 4.1 `pick_day`: прогнать `taken` через `occupied_grid_slots`; вывести список записей дня перед сеткой
- [x] 4.2 `pick_move_day`: то же (occupancy через `occupied_grid_slots` + список дня)
- [x] 4.3 `pick_weekday`: резолвить `next_weekday_on_or_after(today, weekday)`, считать занятость (`series=ctx`, `exclude_slot_id` для `cfg_day`), передать `current` для правки, вывести список дня
- [x] 4.4 `start_slot_time`: считать занятость на ближайшей дате `slot.weekday` с `exclude_slot_id=slot.id` и `current=slot.time_hhmm`, вывести список дня
- [x] 4.5 Убедиться, что `rcreate`/`cfg_add` идут без `exclude_slot_id`/`current` (обычные 🟢/🔴)

## 5. Тесты хендлеров

- [x] 5.1 Регулярное создание слота: сетка показывает занятые слоты по ближайшей дате дня недели (раньше был пустой `set()`)
- [x] 5.2 Правка времени слота: собственный слот — отдельным маркером, чужие — занятыми
- [x] 5.3 Разовая запись и перенос: запись вне сетки помечает оба перекрытых слота
- [x] 5.4 Во всех пикерах перед сеткой присутствует текстовый список записей дня

## 6. Документация и проверка

- [x] 6.1 Обновить `docs/bot.md` (поведение пикеров слотов) и `docs/features.md` (подсветка + список дня)
- [x] 6.2 `openspec validate slot-occupancy-overlap`
- [x] 6.3 `make check` — формат, линт, типы, 100% покрытие
